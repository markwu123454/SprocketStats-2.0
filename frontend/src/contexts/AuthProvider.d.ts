import * as React from "react"

/** Wraps `children` with the auth context (see `authContext.ts` for the value shape). */
export declare function AuthProvider(props: { children: React.ReactNode }): React.JSX.Element
